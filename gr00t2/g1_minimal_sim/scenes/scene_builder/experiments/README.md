# Experiments (CLI entry points)

- **`visualize_glb_mujoco.py`** — preview GLB in MuJoCo (implementation: `scenebuilder.glb_pipeline`).
- **`visualize_usdz_mujoco.py`** — preview USD/USDZ as merged STL mesh.
- **`imports/`** — sample assets (e.g. diner booth GLB).

Run from **`scene_builder/`** root so `import scenebuilder` resolves.
